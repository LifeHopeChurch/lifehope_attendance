<h2>New event occurrence</h2>

<%= render "form.html", changeset: @changeset, events: @events,
                        action: event_occurrence_path(@conn, :create) %>

<%= link "Back", to: event_occurrence_path(@conn, :index) %>
